# This file contains environment variables used by docker-compose
# Passing the file path is done using the --env-file option:
# `docker-compose --env-file ./config/.env.dev up` 
PROJECT_ID=py-rest-service-template



PORT=8000
